interface:
  display_name: "Three.js to EASEL.js"
  short_description: "Audit Three.js migrations to EASEL 0.7"
  default_prompt: "Use $threejs-to-easeljs to inventory an existing Three.js project, route each subsystem against the r185 and EASEL 0.7.0 evidence baseline, and mark source-version-dependent, unsupported, surface-only, UNKNOWN, and UNVERIFIED behavior explicitly."
